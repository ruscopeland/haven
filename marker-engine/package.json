{
  "name": "haven-engine",
  "private": true,
  "version": "1.0.0",
  "description": "Haven trading engine — headless executor for chart markers and live strategies. Runs on the user's machine; keys never leave it.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "setup": "node setup.js",
    "paper-runner": "node paper-runner-service.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.16.0"
  }
}
