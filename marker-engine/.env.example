# Copy to .env (or leave absent - the engine falls back to crypto-wallet/.env VITE_PRIVATE_KEY)
PRIVATE_KEY=
RPC_URL=https://bsc-dataseed.binance.org
API_URL=http://localhost:8000
GAS_PRICE_GWEI=1
SLIPPAGE_PCT=0.5
# Used only when a marker was placed without an amount
QUICK_BUY_PERCENT=5
QUICK_SELL_PERCENT=100
