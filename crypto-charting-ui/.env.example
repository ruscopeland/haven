# Public wallet ADDRESS (not the key!) shown on the Dashboard wallet panel.
# The private key stays in marker-engine/.env only — this app never signs.
VITE_WALLET_ADDRESS=0x0000000000000000000000000000000000000000
